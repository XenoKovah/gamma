from django.apps import AppConfig


class UsersConfig(AppConfig):
    """
    Application Configuration for `users` Application.
    """

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'users'
