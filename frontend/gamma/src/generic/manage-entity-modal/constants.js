export const DEFAULT_FORM_VALUES = {
  title: '',
  description: '',
  isActive: false,
  image: null,
  rules: [],
};

export const DATE_TYPES = {
  START: 'start',
  END: 'end',
};

export const DEFAULT_ACCEPTED_IMAGE_FORMATS = ['image/png', 'image/jpeg', 'image/webp'];

export const MAX_IMAGE_SIZE = 20; // 20MB
