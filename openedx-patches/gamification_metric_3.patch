From 35b51ba2625acc6b62aacf1293d87d5ba06af05c Mon Sep 17 00:00:00 2001
From: xahgmah <xahgmah@yandex.ru>
Date: Tue, 20 Sep 2016 17:46:48 +0300
Subject: [PATCH 1/3] referral link

---
 cms/envs/common.py                                 |  1 +
 common/djangoapps/referrals/__init__.py            |  0
 .../referrals/migrations/0001_initial.py           | 29 ++++++++++++++
 common/djangoapps/referrals/migrations/__init__.py |  0
 common/djangoapps/referrals/models.py              | 19 +++++++++
 common/djangoapps/referrals/signals.py             |  0
 common/djangoapps/referrals/urls.py                | 10 +++++
 common/djangoapps/referrals/utils.py               | 13 ++++++
 common/djangoapps/referrals/views.py               | 46 ++++++++++++++++++++++
 lms/envs/common.py                                 |  1 +
 lms/static/sass/course/courseware/_courseware.scss |  6 +++
 lms/templates/courseware/info.html                 | 23 +++++++++--
 lms/urls.py                                        |  7 ++++
 13 files changed, 152 insertions(+), 3 deletions(-)
 create mode 100644 common/djangoapps/referrals/__init__.py
 create mode 100644 common/djangoapps/referrals/migrations/0001_initial.py
 create mode 100644 common/djangoapps/referrals/migrations/__init__.py
 create mode 100644 common/djangoapps/referrals/models.py
 create mode 100644 common/djangoapps/referrals/signals.py
 create mode 100644 common/djangoapps/referrals/urls.py
 create mode 100644 common/djangoapps/referrals/utils.py
 create mode 100644 common/djangoapps/referrals/views.py

diff --git a/cms/envs/common.py b/cms/envs/common.py
index dcf3d51..8f37b59 100644
--- a/cms/envs/common.py
+++ b/cms/envs/common.py
@@ -819,6 +819,7 @@ INSTALLED_APPS = (
     'lms.djangoapps.verify_student',
 
     'gamification_metric',
+    'referrals',
 )
 
 
diff --git a/common/djangoapps/referrals/__init__.py b/common/djangoapps/referrals/__init__.py
new file mode 100644
index 0000000..e69de29
diff --git a/common/djangoapps/referrals/migrations/0001_initial.py b/common/djangoapps/referrals/migrations/0001_initial.py
new file mode 100644
index 0000000..9404c5c
--- /dev/null
+++ b/common/djangoapps/referrals/migrations/0001_initial.py
@@ -0,0 +1,29 @@
+# -*- coding: utf-8 -*-
+from __future__ import unicode_literals
+
+from django.db import migrations, models
+from django.conf import settings
+import xmodule_django.models
+import referrals.utils
+
+
+class Migration(migrations.Migration):
+
+    dependencies = [
+        migrations.swappable_dependency(settings.AUTH_USER_MODEL),
+    ]
+
+    operations = [
+        migrations.CreateModel(
+            name='Referrals',
+            fields=[
+                ('id', models.AutoField(verbose_name='ID', serialize=False, auto_created=True, primary_key=True)),
+                ('course_id', xmodule_django.models.CourseKeyField(max_length=255, db_index=True)),
+                ('hashkey', models.CharField(default=referrals.utils.hashkey_generator, unique=True, max_length=32)),
+                ('status', models.CharField(default=b'active', max_length=10, choices=[(b'active', b'active'), (b'inactive', b'inactive')])),
+                ('created', models.DateTimeField(db_index=True, auto_now_add=True, null=True)),
+                ('updated', models.DateTimeField(auto_now=True, db_index=True)),
+                ('user', models.ForeignKey(to=settings.AUTH_USER_MODEL)),
+            ],
+        ),
+    ]
diff --git a/common/djangoapps/referrals/migrations/__init__.py b/common/djangoapps/referrals/migrations/__init__.py
new file mode 100644
index 0000000..e69de29
diff --git a/common/djangoapps/referrals/models.py b/common/djangoapps/referrals/models.py
new file mode 100644
index 0000000..e09ad92
--- /dev/null
+++ b/common/djangoapps/referrals/models.py
@@ -0,0 +1,19 @@
+from django.db import models
+from django.contrib.auth.models import User
+from xmodule_django.models import CourseKeyField
+from utils import hashkey_generator
+
+
+class Referrals(models.Model):
+    STATUS_ACTIVE = 'active'
+    STATUS_INACTIVE = 'inactive'
+    STATUSES = [
+        (STATUS_ACTIVE, STATUS_ACTIVE),
+        (STATUS_INACTIVE, STATUS_INACTIVE)
+    ]
+    user = models.ForeignKey(User, db_index=True)
+    course_id = CourseKeyField(max_length=255, db_index=True)
+    hashkey = models.CharField(max_length=32, unique=True, default=hashkey_generator)
+    status = models.CharField(max_length=10, choices=STATUSES, default=STATUS_ACTIVE)
+    created = models.DateTimeField(auto_now_add=True, null=True, db_index=True)
+    updated = models.DateTimeField(auto_now=True, db_index=True)
diff --git a/common/djangoapps/referrals/signals.py b/common/djangoapps/referrals/signals.py
new file mode 100644
index 0000000..e69de29
diff --git a/common/djangoapps/referrals/urls.py b/common/djangoapps/referrals/urls.py
new file mode 100644
index 0000000..c15416c
--- /dev/null
+++ b/common/djangoapps/referrals/urls.py
@@ -0,0 +1,10 @@
+"""
+Django URLs for service status app
+"""
+from django.conf.urls import patterns, url
+from views import user_referral
+
+urlpatterns = patterns(
+    '',
+    url(r'^(?P<hashkey>[^/]*)$', user_referral, name='user_referral'),
+)
diff --git a/common/djangoapps/referrals/utils.py b/common/djangoapps/referrals/utils.py
new file mode 100644
index 0000000..4d1bc1d
--- /dev/null
+++ b/common/djangoapps/referrals/utils.py
@@ -0,0 +1,13 @@
+import hashlib
+from uuid import uuid4
+
+from django.conf import settings
+
+
+def hashkey_generator():
+    """
+    Generate a hashkey for Referral link.
+    """
+    hash = hashlib.sha1(uuid4().hex.encode('utf-8'))
+    hash.update(settings.SECRET_KEY.encode('utf-8'))
+    return hash.hexdigest()[::2]
diff --git a/common/djangoapps/referrals/views.py b/common/djangoapps/referrals/views.py
new file mode 100644
index 0000000..632f2fc
--- /dev/null
+++ b/common/djangoapps/referrals/views.py
@@ -0,0 +1,46 @@
+from rest_framework.response import Response
+from rest_framework.views import APIView
+from rest_framework import permissions
+
+from django.core.urlresolvers import reverse
+from django.utils.decorators import method_decorator
+from django.views.decorators.csrf import csrf_exempt
+from django.shortcuts import redirect, get_object_or_404
+
+from opaque_keys.edx.keys import CourseKey
+from models import Referrals
+from utils import hashkey_generator
+
+
+def user_referral(request, hashkey):
+    referral = get_object_or_404(Referrals, hashkey=hashkey, status=Referrals.STATUS_ACTIVE)
+    request.session['referral'] = {
+        'course_id': unicode(referral.course_id),
+        'user_id': referral.user.pk
+    }
+    course_redirect = reverse('course_root', kwargs={'course_id': unicode(referral.course_id)})
+    return redirect(course_redirect)
+
+
+class GetHashKeyView(APIView):
+    """
+    API response which return referral's hash key for current user and course
+    """
+
+    permission_classes = (permissions.IsAuthenticated,)
+
+    @method_decorator(csrf_exempt)
+    def post(self, request):
+        """
+        Handler for the POST method to this view.
+        """
+
+        referral, created = Referrals.objects.get_or_create(
+            user=request.user,
+            course_id=CourseKey.from_string(request.data.get('course_id')),
+        )
+        if not created:
+            referral.hashkey = hashkey_generator()
+            referral.status = Referrals.STATUS_ACTIVE
+            referral.save()
+        return Response({'hashkey': reverse('referrals:user_referral', kwargs={'hashkey': referral.hashkey})})
diff --git a/lms/envs/common.py b/lms/envs/common.py
index a0e0fc9..4e7b242 100644
--- a/lms/envs/common.py
+++ b/lms/envs/common.py
@@ -1943,6 +1943,7 @@ INSTALLED_APPS = (
     'sorl.thumbnail',
 
     'gamification_metric',
+    'referrals',
 )
 
 # Migrations which are not in the standard module "migrations"
diff --git a/lms/static/sass/course/courseware/_courseware.scss b/lms/static/sass/course/courseware/_courseware.scss
index fe9ab9f..acfb371 100644
--- a/lms/static/sass/course/courseware/_courseware.scss
+++ b/lms/static/sass/course/courseware/_courseware.scss
@@ -649,3 +649,9 @@ section.self-assessment {
     font-weight: bold;
   }
 }
+
+.course-referral {
+  .course-referral-link {
+    display: none;
+  }
+}
\ No newline at end of file
diff --git a/lms/templates/courseware/info.html b/lms/templates/courseware/info.html
index 0b9b20f..70c55ed 100644
--- a/lms/templates/courseware/info.html
+++ b/lms/templates/courseware/info.html
@@ -38,9 +38,21 @@ from openedx.core.djangoapps.self_paced.models import SelfPacedConfiguration
 <%block name="js_extra">
   <script type="text/javascript" src="${static.url('js/jquery.treeview.js')}"></script>
   <script type="text/javascript" charset="utf-8">
-$(document).ready(function(){
-    $(".handouts").treeview({collapsed:true, unique:true/*, cookieId: "treeview-book-nav", persist: "cookie"*/});
-    });
+      $(document).ready(function () {
+          var domain_address = document.location.origin
+          $(".handouts").treeview({
+              collapsed: true,
+              unique: true/*, cookieId: "treeview-book-nav", persist: "cookie"*/
+          });
+          $(".course-referral-button").click(function () {
+              $.post(domain_address + "/api/get_referral_hash_key/", {'course_id': '${str(course_id)}'},
+                  function (data) {
+                      $('.course-referral-link').val(domain_address + data.hashkey).show();
+                      document.execCommand("copy");
+                  });
+
+          });
+      });
   </script>
 </%block>
 
@@ -59,6 +71,11 @@ $(document).ready(function(){
 
       <h1>${_("Course Updates &amp; News")}</h1>
       ${get_course_info_section(request, course, 'updates')}
+
+    <div class="course-referral">
+        <button class="course-referral-button">Invite friend</button>
+        <input class="course-referral-link" type="text" size="70" disabled>
+    </div>
     </section>
     <section aria-label="${_('Handout Navigation')}" class="handouts">
       % if False:
diff --git a/lms/urls.py b/lms/urls.py
index 7ea3092..5fb0af6 100644
--- a/lms/urls.py
+++ b/lms/urls.py
@@ -11,6 +11,7 @@ from django.conf.urls.static import static
 import django.contrib.auth.views
 from microsite_configuration import microsite
 import auth_exchange.views
+import referrals.views
 
 from config_models.views import ConfigurationModelCurrentAPIView
 from openedx.core.djangoapps.programs.models import ProgramsApiConfig
@@ -108,6 +109,12 @@ urlpatterns = (
 
     url(r'^api/commerce/', include('commerce.api.urls', namespace='commerce_api')),
     url(r'^api/credit/', include('openedx.core.djangoapps.credit.urls', app_name="credit", namespace='credit')),
+    url(r'referral/', include('referrals.urls', app_name="referrals", namespace='referrals')),
+    url(
+        r'^api/get_referral_hash_key/',
+        referrals.views.GetHashKeyView.as_view(),
+        name="get_hash_key"
+    )
 )
 
 if settings.FEATURES["ENABLE_COMBINED_LOGIN_REGISTRATION"]:
-- 
2.7.4


From b61e5c1d90840227859e5a41f9ad6e3013d75f3c Mon Sep 17 00:00:00 2001
From: Maksim Sokolskiy <misokolsky@gmail.com>
Date: Wed, 21 Sep 2016 12:45:12 +0300
Subject: [PATCH 2/3] feat(referrals): add `ActivatedLinks` to track referrals

---
 common/djangoapps/gamification_metric/signals.py   | 15 +++++++++++++
 .../referrals/migrations/0002_activatedlinks.py    | 25 ++++++++++++++++++++++
 common/djangoapps/referrals/models.py              |  6 ++++++
 common/djangoapps/referrals/signals.py             | 16 ++++++++++++++
 common/djangoapps/referrals/views.py               | 17 ++++++++++-----
 5 files changed, 74 insertions(+), 5 deletions(-)
 create mode 100644 common/djangoapps/referrals/migrations/0002_activatedlinks.py

diff --git a/common/djangoapps/gamification_metric/signals.py b/common/djangoapps/gamification_metric/signals.py
index 4226290..1db1866 100644
--- a/common/djangoapps/gamification_metric/signals.py
+++ b/common/djangoapps/gamification_metric/signals.py
@@ -8,6 +8,7 @@ from django.dispatch import receiver
 
 from tasks import send_api_request
 from certificates.models import CertificateStatuses
+from referrals.models import ActivatedLinks
 
 
 @receiver(post_save, sender='courseware.StudentModule')
@@ -38,6 +39,20 @@ def send_enroll_achievement(sender, instance, created, **kwargs):
         }
         send_api_request(data)
 
+        activated_link = ActivatedLinks.objects.filter(
+            user=instance.user,
+            referral__course_id=course_id,
+            used=False
+        )
+        if activated_link:
+            data = {
+                'username': activated_link.referral.user.username,
+                'course_id': activated_link.referral.course_id,
+                'event_type': 'referrer',
+                'uid': '{}_{}_{}'.format(activated_link.referral.user.pk, activated_link.referral.course_id, 'referrer'),
+            }
+            send_api_request(data)
+
 
 @receiver(post_save, sender='certificates.GeneratedCertificate')
 def send_certificate_generation(sender, instance, created, **kwargs):
diff --git a/common/djangoapps/referrals/migrations/0002_activatedlinks.py b/common/djangoapps/referrals/migrations/0002_activatedlinks.py
new file mode 100644
index 0000000..4b62fe4
--- /dev/null
+++ b/common/djangoapps/referrals/migrations/0002_activatedlinks.py
@@ -0,0 +1,25 @@
+# -*- coding: utf-8 -*-
+from __future__ import unicode_literals
+
+from django.db import migrations, models
+from django.conf import settings
+
+
+class Migration(migrations.Migration):
+
+    dependencies = [
+        migrations.swappable_dependency(settings.AUTH_USER_MODEL),
+        ('referrals', '0001_initial'),
+    ]
+
+    operations = [
+        migrations.CreateModel(
+            name='ActivatedLinks',
+            fields=[
+                ('id', models.AutoField(verbose_name='ID', serialize=False, auto_created=True, primary_key=True)),
+                ('used', models.BooleanField(default=False)),
+                ('referral', models.ForeignKey(to='referrals.Referrals')),
+                ('user', models.ForeignKey(to=settings.AUTH_USER_MODEL)),
+            ],
+        ),
+    ]
diff --git a/common/djangoapps/referrals/models.py b/common/djangoapps/referrals/models.py
index e09ad92..0a72d4a 100644
--- a/common/djangoapps/referrals/models.py
+++ b/common/djangoapps/referrals/models.py
@@ -17,3 +17,9 @@ class Referrals(models.Model):
     status = models.CharField(max_length=10, choices=STATUSES, default=STATUS_ACTIVE)
     created = models.DateTimeField(auto_now_add=True, null=True, db_index=True)
     updated = models.DateTimeField(auto_now=True, db_index=True)
+
+
+class ActivatedLinks(models.Model):
+    referral = models.ForeignKey(Referrals)
+    user = models.ForeignKey(User)
+    used = models.BooleanField(default=False)
diff --git a/common/djangoapps/referrals/signals.py b/common/djangoapps/referrals/signals.py
index e69de29..cdf7d2d 100644
--- a/common/djangoapps/referrals/signals.py
+++ b/common/djangoapps/referrals/signals.py
@@ -0,0 +1,16 @@
+from django.contrib.auth.signals import user_logged_in
+
+from .models import ActivatedLinks, Referrals
+
+
+def add_referral(sender, user, request, **kwargs):
+    referral_info = request.session['referral']
+    if referral_info and referral_info.get('referral_id'):
+        referral = Referrals.objects.filter(id=referral_info['referral_id'])
+        ActivatedLinks.objects.get_or_create(
+            referral=referral,
+            user=request.user
+        )
+
+
+user_logged_in.connect(add_referral)
diff --git a/common/djangoapps/referrals/views.py b/common/djangoapps/referrals/views.py
index 632f2fc..e683634 100644
--- a/common/djangoapps/referrals/views.py
+++ b/common/djangoapps/referrals/views.py
@@ -8,16 +8,23 @@ from django.views.decorators.csrf import csrf_exempt
 from django.shortcuts import redirect, get_object_or_404
 
 from opaque_keys.edx.keys import CourseKey
-from models import Referrals
+from models import Referrals, ActivatedLinks
 from utils import hashkey_generator
 
 
 def user_referral(request, hashkey):
     referral = get_object_or_404(Referrals, hashkey=hashkey, status=Referrals.STATUS_ACTIVE)
-    request.session['referral'] = {
-        'course_id': unicode(referral.course_id),
-        'user_id': referral.user.pk
-    }
+    if not request.user.is_authenticated():
+        request.session['referral'] = {
+            'course_id': unicode(referral.course_id),
+            'user_id': referral.user.pk,
+            'referral_id': referral.id
+        }
+    else:
+        ActivatedLinks.objects.get_or_create(
+            referral=referral,
+            user=request.user
+        )
     course_redirect = reverse('course_root', kwargs={'course_id': unicode(referral.course_id)})
     return redirect(course_redirect)
 
-- 
2.7.4


From efe97610f1d9321df799bcdb67369f61fc2aa4ce Mon Sep 17 00:00:00 2001
From: xahgmah <xahgmah@yandex.ru>
Date: Wed, 21 Sep 2016 14:04:46 +0300
Subject: [PATCH 3/3] referral api call on enroll

---
 common/djangoapps/gamification_metric/signals.py | 14 +++++++++-----
 1 file changed, 9 insertions(+), 5 deletions(-)

diff --git a/common/djangoapps/gamification_metric/signals.py b/common/djangoapps/gamification_metric/signals.py
index 1db1866..5cc14e4 100644
--- a/common/djangoapps/gamification_metric/signals.py
+++ b/common/djangoapps/gamification_metric/signals.py
@@ -6,6 +6,7 @@ import json
 from django.db.models.signals import post_save
 from django.dispatch import receiver
 
+from opaque_keys.edx.keys import CourseKey
 from tasks import send_api_request
 from certificates.models import CertificateStatuses
 from referrals.models import ActivatedLinks
@@ -41,18 +42,21 @@ def send_enroll_achievement(sender, instance, created, **kwargs):
 
         activated_link = ActivatedLinks.objects.filter(
             user=instance.user,
-            referral__course_id=course_id,
+            referral__course_id=CourseKey.from_string(course_id),
             used=False
-        )
+        ).first()
         if activated_link:
+            uid = '{}_{}_{}'.format(activated_link.referral.user.pk, activated_link.referral.course_id, 'referrer')
             data = {
                 'username': activated_link.referral.user.username,
-                'course_id': activated_link.referral.course_id,
+                'course_id': course_id,
                 'event_type': 'referrer',
-                'uid': '{}_{}_{}'.format(activated_link.referral.user.pk, activated_link.referral.course_id, 'referrer'),
+                'uid': uid
             }
             send_api_request(data)
-
+            activated_link.used = True
+            # TODO uncomment this after debug finishing
+            # activated_link.save()
 
 @receiver(post_save, sender='certificates.GeneratedCertificate')
 def send_certificate_generation(sender, instance, created, **kwargs):
-- 
2.7.4

